# Home-page photo wall

`home-1.jpg` … `home-5.jpg` feed the sprinkled polaroids on the home page
(the `.polaroid-band` section in `plush-body.html`).

These are placeholder gradients. Replace each file with a real square-ish
team photo, keeping the same filename. ~600–900 px, JPEG, a few hundred KB
each is plenty. Update the `<figcaption>` text in `plush-body.html` to match.
